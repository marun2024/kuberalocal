meta {
  name: Get Contract
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}{{apiVersion}}/contracts/{{contractId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

headers {
  Host: {{tenantHost}}
}

tests {
  test("Get contract successful", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.have.property('id');
    expect(res.getBody()).to.have.property('title');
  });
}
