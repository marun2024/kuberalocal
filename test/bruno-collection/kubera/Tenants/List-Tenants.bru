meta {
  name: List Tenants
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}{{apiVersion}}/tenants
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("List tenants successful", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an('array');
  });
}